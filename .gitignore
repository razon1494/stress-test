__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.ruff_cache/
.pytest_cache/
.ipynb_checkpoints/

# data & results are regenerated by the pipeline; only manifests are versioned
data/raw/
data/processed/
data/transformed/
results/cache/
*.parquet
wandb/

# external corpora: ICNALE license prohibits redistribution; Liang et al.'s
# repo has no LICENSE file either, so neither is committed — see
# src/stress_test/data/non_native.py
data/external/

# fine-tuned model weights (regenerate via scripts/08_finetune_deberta.py)
models/
annotation/

# Local reading copies of third-party papers; cite or link them instead of
# redistributing copyrighted PDFs in this repository.
docs/Related Papers/

# Local LaTeX build products (GitHub Actions publishes the reviewed PDF artifact).
paper/Stress_Test.pdf
paper/main.pdf
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.fdb_latexmk
paper/*.fls
paper/*.log
paper/*.out
